#include "resource_handler.h"

// =================================================================================================
// GfxResourceHandler — Vulkan implementation
//
// All logic lives inline in the header (Track / Cleanup / FrameIndex). The .cpp exists as a
// translation-unit anchor so the singleton instance has a .obj home.

// =================================================================================================
